class Solution(object):
    def containsDuplicate(self, nums):
        print(set(nums))
        if len(nums)==0:
            return False
        elif sorted(nums)==sorted(list(set(nums))):
            return False
        else:
            return True
