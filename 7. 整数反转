class Solution(object):
    def reverse(self, x):
        if x<0:
            a=int(str(-x)[::-1])*-1        
        else:
            a=int(str(x)[::-1])
        if a >=-2**31 and a<=2**31-1:
            return a
        else:
            return 0
